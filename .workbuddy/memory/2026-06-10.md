# 2026-06-10 工作日志

## 功能增强：能力查询路由 + 动态工具列表注入
- `workflow.py` `PURE_LLM_KEYWORDS` 新增：`"有什么功能"`, `"你的功能"`, `"你的工具"`, `"有什么工具"`
- `generator.py` 新增 `_build_pure_llm_prompt()`：运行时从 `ToolRegistry.list_tools(enabled_only=True)` 动态拉取工具列表，拼入 Pure LLM 系统提示词
- 弃用静态 `PURE_LLM_SYSTEM_PROMPT`，拆为 `PURE_LLM_BASE_PROMPT` + `PURE_LLM_TOOLS_HEADER` + `PURE_LLM_TAIL_PROMPT` 三段

## 前端改造
- `main.py`：页面标题改为"企业办公数据助手 AI Agent"，system prompt 同步修改

## 系统架构图
- 生成 `output/architecture_diagram.html`：五层 SVG 架构图（入口层 → LangGraph 状态机 → 四路由分支 → 支撑系统 → 输出层）
- 覆盖：Gradio/FastAPI 入口、router_node 路由、Pure LLM/RAG/ReAct/Tools 四分支完整流程、Memory 系统、ToolRegistry、LLM 层、Config
- 第二版修复重叠问题，画布扩至 1600×2480，五层严格分区、四分支横向并排

## Bug 修复：自我介绍遗漏 RAG 能力
- 根因：`PURE_LLM_BASE_PROMPT` 只写了基础能力，RAG/ReAct 是路由分支不是工具，不在 ToolRegistry 中
- `PURE_LLM_BASE_PROMPT` 新增"你的系统能力"段：RAG 检索增强、ReAct 推理循环、Web Search、数据处理
- `PURE_LLM_TAIL_PROMPT` 第3条改为引导 LLM 同时列出系统能力和工具
