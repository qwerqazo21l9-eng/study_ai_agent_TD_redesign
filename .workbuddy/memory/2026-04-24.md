# 2026-04-24 工作日志

## 完成内容
1. Web Search 测试 - Tavily API 验证成功
2. ReAct 循环 Bug 修复
   - `src/agent/react/tools.py`: 添加 await 到 `retrieve()` 调用
   - `src/agent/react/agent.py` + `tools.py`: 修复 LangChain 返回格式兼容问题

## 遇到的问题
- `'coroutine' object has no attribute 'get'` - retrieve 是 async 但没 await
- `'str' object has no attribute 'content'` - LangChain 返回格式问题
- `'Action' object has no attribute 'get'` - 添加防御性类型检查

## Bug 修复记录
- `workflow.py`: 添加 `isinstance(result, dict)` 检查
- `agent.py`: action_history 序列化兼容处理
- `agent.py`: _process_action_result 增加多种返回类型兼容
- `agent.py`: _process_action_result 完整重构，防御性处理 result 类型
- `prompts.py`: 修复 Action 对象访问方式
  - `build_observe_prompt`: `action['action_type']` → `action.action_type.value`
  - `build_think_prompt`: 同上
- **核心 Bug 已修复**：`_think` 和 `_generate_final` 方法中 `agenerate()` 调用格式错误
  - 原因：`ChatOpenAI.agenerate()` 期望 `list[list[BaseMessage]]`，不是 `list[dict]`
  - 修复：使用 `HumanMessage`/`SystemMessage` 对象，格式 `[[HumanMessage(...), ...]]`
  - 关键点：`agenerate` 期望双层括号，外层是对话列表，内层是消息列表

## ReAct Prompt 重构 - 轨迹格式（当日第四次）
- 根因：`build_think_prompt` 只把文档内容和数量摘要给 LLM，LLM 无法判断"这些文档能不能回答问题"
- 正确做法：每轮 Thought → Action → Observation 按轨迹格式拼成完整的推理历史
- 修改：`prompts.py` THINK_TEMPLATE + `build_think_prompt` 重写为标准 ReAct 轨迹格式
- `_generate_final` 增加推理轨迹摘要，帮助最终生成 LLM 了解信息来源
- 预期效果：LLM 读完轨迹后能自主判断"本地有了→还需要联网→联网有了→可以生成"
